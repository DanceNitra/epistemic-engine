#!/usr/bin/env bash
# epistemic-engine wrapper — ensures correct venv
DIR="$(cd "$(dirname "$0")" && pwd)"
if [ -f "$DIR/.venv/bin/python3" ]; then
    exec "$DIR/.venv/bin/python3" "$DIR/engine.py" "$@"
else
    exec python3 "$DIR/engine.py" "$@"
fi